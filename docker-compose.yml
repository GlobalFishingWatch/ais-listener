version: '3.9'
services:
  server:
    entrypoint:  [ "python", "-u", "main.py", "-vv", "server" ]
    ports:
      - "10110:10110/udp"
    build:
      context: .
    volumes:
      - ".:/opt/code"
      - "gcp:/root/.config/"

  gcloud:
    entrypoint: gcloud
    build:
      context: .
    volumes:
      - ".:/opt/code"
      - "gcp:/root/.config/"

  bash:
    entrypoint: /bin/bash
    build:
      context: .
    volumes:
      - ".:/opt/code"
      - "gcp:/root/.config/"

volumes:
  gcp:
    external: true
